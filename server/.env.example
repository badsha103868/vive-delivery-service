# Copy this file to `.env` and replace placeholders with real values.
# Local example:
# MONGO_URI=mongodb://username:password@localhost:27017/vive_delivery
#
# Atlas (mongodb+srv) example:
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.abcd123.mongodb.net/vive_delivery?retryWrites=true&w=majority
#
# Notes:
# - If your password contains special characters, URL-encode it: encodeURIComponent('p@ss/w:rd')
# - For Atlas: ensure 'Network Access' -> IP Whitelist includes your dev IP (or 0.0.0.0/0 for testing).
# - Create a DB user in Atlas with appropriate roles.
#
PORT=4000
MONGO_URI=mongodb://username:password@localhost:27017/vive_delivery
MONGO_DB=vive_delivery
JWT_SECRET=replace_with_a_long_random_value
